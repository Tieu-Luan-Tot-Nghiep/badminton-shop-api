package com.badminton.shop.modules.promotion.entity;

public enum DiscountType {
    PERCENTAGE,      // Giảm theo phần trăm (%)
    FIXED_AMOUNT,    // Giảm theo số tiền cố định (VNĐ)
    FREE_SHIP        // Miễn phí vận chuyển (Tùy chọn thêm)
}
